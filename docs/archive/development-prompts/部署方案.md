# FilmMirror 部署到 GitHub Pages（自动同步方案）

## 为什么选 GitHub Pages

| 对比 | Vercel | GitHub Pages |
|------|--------|-------------|
| 注册 | 需要新注册（你注册不了） | **你已有 GitHub 账号** ✅ |
| 费用 | 免费 | **免费** ✅ |
| 同步 | 推代码自动部署 | **推代码自动部署** ✅ |
| 有效期 | 不活跃可能下线 | **永久有效** ✅ |
| 自定义域名 | 支持 | 支持 |
| 稳定性 | 偶尔国内访问慢 | **国内可访问** ✅ |

---

## 执行步骤

### 第 1 步：配置 vite.config.js

打开 `vite.config.js`，**完整替换**为以下内容：

```js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: '/film-mirror/',
  server: {
    host: '0.0.0.0',
    port: 3000,
    open: true,
  },
});
```

注意：`base: '/film-mirror/'` 这行是关键！GitHub Pages 的 URL 是 `https://你的用户名.github.io/film-mirror/`，资源文件必须带这个前缀才能加载。如果仓库名不是 film-mirror，请替换为实际仓库名。

### 第 2 步：在 GitHub 上创建仓库

1. 打开浏览器，访问 **https://github.com/new**
2. Repository name 填：`film-mirror`
3. 选 **Public**（公开仓库才能用免费的 GitHub Pages）
4. **不要勾选** "Add a README file"、"Add .gitignore"、"Choose a license"
5. 点 **Create repository**

### 第 3 步：在终端中连接 GitHub 并推送代码

在项目目录的终端中，依次执行以下命令：

```bash
# 初始化 git（如果还没有）
git init

# 设置远程仓库（把 lllu 替换成你的 GitHub 用户名）
git remote add origin https://github.com/lllu/film-mirror.git

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: FilmMirror project"

# 推送到 GitHub
git branch -M main
git push -u origin main
```

如果 push 时弹出 GitHub 登录窗口，请在浏览器中完成授权。

### 第 4 步：在 GitHub 上开启 Pages

1. 打开 **https://github.com/lllu/film-mirror/settings/pages**
2. Source 选择 **GitHub Actions**
3. 保存

### 第 5 步：验证部署

1. 打开 **https://github.com/lllu/film-mirror/actions**
2. 等待 "Deploy to GitHub Pages" 这个 workflow 变成 ✅ 绿色
3. 打开 **https://lllu.github.io/film-mirror/** 查看网站

---

## 以后怎么同步更新

**改完代码后，只需要 2 条命令：**

```bash
git add .
git commit -m "描述你改了什么"
git push
```

push 之后，GitHub Actions 会**自动重新构建并部署**，大约 1-2 分钟后生效。

你不需要手动 build，不需要手动上传——**push 即部署**。

---

## 如果需要 Claude Code 来做

让 Claude Code 执行以下操作：

1. 修改 vite.config.js，添加 `base: '/film-mirror/'`
2. 帮我创建 GitHub 仓库并推送代码（用 gh 命令或 git 命令）
3. 开启 GitHub Pages（用 gh api 命令）

Claude Code 可以自动完成以上所有步骤，包括创建仓库和开启 Pages。

---

## 最终效果

- 🌐 网站地址：`https://你的GitHub用户名.github.io/film-mirror/`
- 🔄 同步方式：`git push` → 自动部署
- 💰 费用：永久免费
- ⏳ 有效期：永久有效（只要 GitHub 还在）
- 📱 手机访问：✅ 支持
- 🔗 放简历：✅ 可以
