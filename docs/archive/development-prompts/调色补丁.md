# FilmMirror 色调调整 — 独立补丁（仅改颜色）

当前页面布局已经OK（衬线标题、玻璃卡片、PROFILE/TODAY符号都有了），只需要把色调调亮。背景目前还是近纯黑，太暗了。

---

## 只需要做一件事：替换所有颜色的具体数值

### 第 1 步：修改 index.css 中的 :root 变量

找到 `:root { ... }` 块，**整体替换**为以下值。注意每个数值都比之前亮很多：

```css
:root {
  /* 背景层次 — 关键！比之前亮很多，肉眼可见的区别 */
  --bg-deep: #1c2230;
  --bg-base: #232b3c;
  --bg-card: #2e3749;
  --bg-elevated: #394356;
  --bg-hover: #434e62;

  /* 文字 — 暖色米白 */
  --text-bright: #ede5d6;
  --text-primary: #cec5af;
  --text-secondary: #8e9097;
  --text-muted: #60646d;
  --text-faint: #40454f;

  /* 强调色 — 温暖金 */
  --gold: #c9a86c;
  --gold-light: #dfc49a;
  --gold-dim: #8b7345;
  --gold-glow: rgba(201,168,108,0.15);
  --gold-glow-strong: rgba(201,168,108,0.3);

  /* 边框 */
  --border-subtle: rgba(206,197,175,0.1);
  --border-default: rgba(206,197,175,0.18);
  --border-hover: rgba(206,197,175,0.28);

  /* 玻璃 */
  --glass-bg: rgba(46,55,73,0.65);
  --glass-border: rgba(206,197,175,0.12);
  --glass-blur: blur(24px);

  /* 字体（保持不变）*/
  --font-serif-zh: 'Noto Serif SC', 'Source Han Serif CN', serif;
  --font-serif-en: 'Cormorant Garamond', 'Playfair Display', Georgia, serif;
  --font-sans: 'Inter', 'Noto Sans SC', sans-serif;

  /* 圆角、过渡等保持不变 */
}
```

### 第 2 步：修改 body 背景色

确保 `body { background: var(--bg-deep); }` 使用的是上面的新值。

### 第 3 步：修改首页 HomePage 容器背景渐变

找到 `.home-page-container` 的 background 属性，替换为：

```css
background:
  radial-gradient(ellipse 80% 60% at 50% 0%, rgba(201,168,108,0.08) 0%, transparent 60%),
  radial-gradient(ellipse 60% 40% at 80% 80%, rgba(100,80,160,0.06) 0%, transparent 50%),
  radial-gradient(ellipse 50% 30% at 20% 70%, rgba(80,120,160,0.06) 0%, transparent 50%),
  linear-gradient(180deg, #1c2230 0%, #232b3c 40%, #262e3f 100%);
```

### 第 4 步：修改 .home-card 卡片样式

```css
.home-card {
  background: var(--glass-bg);
  backdrop-filter: blur(20px);
  border: 1px solid var(--glass-border);
}

.home-card:hover {
  border-color: rgba(201,168,108,0.25);
  box-shadow:
    0 16px 48px rgba(15,20,30,0.35),
    0 0 32px rgba(201,168,108,0.1),
    inset 0 1px 0 rgba(206,197,175,0.08);
}
```

### 第 5 步：修改 .sticky-bottom 底部栏背景

```css
.sticky-bottom {
  background: rgba(28,34,48,0.92);
}
```

### 第 6 步：检查所有页面是否有硬编码的深色值

全局搜索以下旧值并全部替换为新值：

| 搜索 | 替换为 |
|------|--------|
| `#09090c` | `#1c2230` |
| `#0d1117` | `#232b3c` |
| `#141920` / `#161b22` | `#2e3749` |
| `#1c2129` | `#394356` |
| `rgba(9,9,12,` | `rgba(28,34,48,` |
| `rgba(13,17,23,` | `rgba(35,43,60,` |
| `#e6edf3` | `#ede5d6` |
| `#8b949e` | `#8e9097` |
| `#6e7681` / `#545d68` | `#60646d` |

---

## 完成

改完后刷新浏览器确认：背景应该是**明显的深蓝灰色**，不再是接近纯黑。卡片和背景之间应该有**清晰的明暗层次区分**。
