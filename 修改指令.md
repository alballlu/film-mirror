# FilmMirror 修改指令清单 v3（配色调亮版）

请逐一完成以下所有修改。**特别注意：视觉部分的每一条都必须严格按照给定的 CSS 值执行，不要自行发挥。配色已参考 Letterboxd 和 MUBI 调亮，不再用近纯黑背景。**

---

## 一、Bug 修复

### 【1】movies.json 格式错误
读取 src/data/movies.json，检查并修复：
- 找到 `"year":1995"` 这种值多了引号的格式错误，改为 `"year":1995`
- 删除任何重复 id 的条目（如重复的花样年华、一一等）
- 确保整个文件是合法 JSON（方括号闭合、逗号正确）
- 修完后告诉我总共有多少部电影

### 【2】海报方案：全部使用首字卡片，不接任何外部图片 API
**重要：不接入 TMDB、不使用任何外部海报 URL。**

请执行以下操作：

#### 2.1 movies.json
所有电影的 poster 字段统一改为空字符串 `""`。

#### 2.2 首字卡片组件（全局统一风格）
在 MovieSelection.jsx 中，把 movie-poster-error 的样式改为以下精确规格：
- 容器：宽100%、高180px、背景 `linear-gradient(145deg, #232838 0%, #262d40 50%, #1e2435 100%)`
- 首字：字号 64px、字体 `'Noto Serif SC', serif`、颜色 `#c9a86c`（金色）、font-weight: 700
- 标题：字号 14px、颜色 `#d4c5b0`、margin-top: 10px
- 导演名：字号 11px、颜色 `#8a8f9e`、margin-top: 4px
- 圆角：16px
- 边框：1px solid rgba(255,255,255,0.06)
- flex 纵向居中排列

已选中态：
- 边框变为 1.5px solid #c9a86c
- box-shadow: 0 0 20px rgba(201,168,108,0.15), inset 0 0 20px rgba(201,168,108,0.05)

Hover 态：
- transform: translateY(-4px)
- box-shadow: 0 12px 40px rgba(0,0,0,0.4), 0 0 0 1px rgba(201,168,108,0.15)
- transition: all 0.3s cubic-bezier(0.4,0,0.2,1)

#### 2.3 Recommendation.jsx 和 DailyResult.jsx 的电影展示
同样用首字卡片风格（参考上面规格），不要用 img 标签加载任何外部图片。

### 【3】MovieSelection.jsx 分类过滤逻辑不完整
当前过滤只做 `tag === cat.key` 精确匹配。请改为每个分类对应一组关键词做 includes 匹配：
- `'科幻'` → `['科幻','太空','未来','人工智能']`
- `'悬疑'` → `['悬疑','犯罪','惊悚','推理','黑色']`
- `'爱情'` → `['爱情','浪漫','暧昧','暗恋']`
- `'公路'` → `['公路','冒险','旅行','旅途']`
- `'青春'` → `['青春','成长','校园','少年']`
- `'社会'` → `['社会','阶级','体制','权威质疑','现实']`
- `'动画'` → `['动画','动漫','皮克斯','宫崎骏']`

---

## 二、新功能：搜索框

### 【4】在 MovieSelection.jsx 页面添加搜索功能
在分类标签栏的右侧（或下方独立一行）添加搜索输入框：
- HTML 结构：一个带图标的输入框容器
- placeholder："搜索电影 / 导演..."
- 支持按电影中文标题、英文名、导演名实时模糊搜索（toLowerCase().includes()）
- 搜索和分类过滤同时生效取交集
- 样式：
  - 背景：rgba(21,25,38,0.8)
  - 边框：1px solid rgba(255,255,255,0.08)
  - 圆角：12px
  - padding: 12px 16px 12px 42px
  - 文字色：var(--text-primary)
  - 字体：14px 'Inter', sans-serif
  - 聚焦时边框变为 var(--gold) + box-shadow: 0 0 0 3px var(--gold-glow)
  - 搜索图标用 SVG 绝对定位在左侧（颜色 var(--text-muted)）
  - 有内容时右侧显示 "×" 清除按钮（圆形背景 var(--border-subtle)，hover 变 var(--border-hover)）
- 搜索为空时显示 "未找到匹配的电影" 居中提示文字（颜色 var(--text-muted)，18px）

---

## 三、视觉全面升级 — 关键！必须严格执行！

当前首页效果非常差：近纯黑背景、两个普通方块卡片、emoji 图标、没有氛围感。
以下每条都给出精确 CSS 值，**请逐行照着改，不要自己发挥。配色已参考 Letterboxd(#14181C) 和 MUBI 调亮，背景用深蓝灰暖色，不是纯黑。**

### 【5】index.html — 引入 Google Fonts
在 `<head>` 中添加（放在 `<title>` 之前）：
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&family=Inter:wght@300;400;500;600&family=Noto+Sans+SC:wght@300;400;500;600&family=Noto+Serif+SC:wght@400;500;600;700&display=swap" rel="stylesheet">
```

### 【6】CSS 全局变量重写（index.css 的 :root）

**完全替换现有的 :root 为以下值（已参考 Letterboxd 和 MUBI 调亮，不用近纯黑）：**

```css
:root {
  /* 背景层次 — 深蓝灰暖色，不是纯黑！参考 Letterboxd #14181C */
  --bg-deep: #151926;
  --bg-base: #1a1e2a;
  --bg-card: #232838;
  --bg-elevated: #2c3245;
  --bg-hover: #353b50;

  /* 文字 — 暖灰米色，不是冷蓝灰 */
  --text-bright: #ede6d8;
  --text-primary: #d4c5b0;
  --text-secondary: #8a8f9e;
  --text-muted: #5c6172;
  --text-faint: #3d4255;

  /* 强调色 — 温暖金（保持不变） */
  --gold: #c9a86c;
  --gold-light: #dfc49a;
  --gold-dim: #8b7345;
  --gold-glow: rgba(201,168,108,0.12);
  --gold-glow-strong: rgba(201,168,108,0.25);

  /* 边框与分割线 */
  --border-subtle: rgba(212,197,176,0.08);
  --border-default: rgba(212,197,176,0.14);
  --border-hover: rgba(212,197,176,0.22);

  /* 玻璃 */
  --glass-bg: rgba(35,40,56,0.65);
  --glass-border: rgba(212,197,176,0.1);
  --glass-blur: blur(24px);

  /* 字体 */
  --font-serif-zh: 'Noto Serif SC', 'Source Han Serif CN', 'STSong', serif;
  --font-serif-en: 'Cormorant Garamond', 'Playfair Display', Georgia, serif;
  --font-sans: 'Inter', 'Noto Sans SC', -apple-system, sans-serif;

  /* 圆角 */
  --radius-sm: 8px;
  --radius-md: 14px;
  --radius-lg: 20px;
  --radius-xl: 28px;

  /* 过渡 */
  --ease-out: cubic-bezier(0.16,1,0.3,1);
  --ease-in-out: cubic-bezier(0.65,0,0.35,1);
}
```

### 【7】body 和 html 全局基础样式（追加到 index.css）

```css
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  font-size: 16px;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

body {
  font-family: var(--font-sans);
  background: var(--bg-deep);
  color: var(--text-primary);
  min-height: 100vh;
  overflow-x: hidden;
  line-height: 1.6;
}
```

每个页面的最外层容器加上 `className="page-enter"` class。

### 【8】HomePage.jsx 视觉重做（最重要！）

这是用户第一眼看到的页面，当前效果极差。请完全按照以下规格重写：

#### 8.1 背景层
HomePage 的根容器 div 加上以下样式：
```css
.home-page-container {
  position: relative;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60px 24px;
  overflow: hidden;
  /* 多层渐变背景 — 深蓝灰暖色，不是纯黑！参考 Letterboxd */
  background:
    radial-gradient(ellipse 80% 60% at 50% 0%, rgba(201,168,108,0.06) 0%, transparent 60%),
    radial-gradient(ellipse 60% 40% at 80% 80%, rgba(100,80,160,0.04) 0%, transparent 50%),
    radial-gradient(ellipse 50% 30% at 20% 70%, rgba(80,120,160,0.04) 0%, transparent 50%),
    linear-gradient(180deg, #151926 0%, #1a1e2a 40%, #1c2130 100%);
}
```

#### 8.2 背景装饰元素（增加层次感！）
在背景上添加几个微弱的装饰元素（用 div 实现，pointer-events: none）：
- 左上方：一个 300x300px 的圆，径向渐变 rgba(201,168,108,0.05) 到 transparent，模糊 80px
- 右下方：一个 200x200px 的圆，径向渐变 rgba(120,100,180,0.04) 到 transparent，模糊 60px
这些装饰让画面不再是一块死黑。

#### 8.3 主标题 "FilmMirror"
```css
.home-title {
  font-family: var(--font-serif-en);
  font-size: clamp(40px, 6vw, 64px);
  font-weight: 600;
  letter-spacing: 0.15em;
  color: var(--text-bright);
  text-align: center;
  margin-bottom: 12px;
  /* 微妙的文字阴影，不是生硬的投影 */
  text-shadow: 0 2px 20px rgba(201,168,108,0.1);
  animation: fadeUp 0.8s var(--ease-out) both;
}
```

#### 8.4 副标题 "电影镜像 · 通过电影读懂自己"
```css
.home-subtitle {
  font-family: var(--font-sans);
  font-size: clamp(14px, 1.5vw, 16px);
  font-weight: 300;
  color: var(--text-muted);
  letter-spacing: 0.3em;
  text-align: center;
  margin-bottom: 72px;
  animation: fadeUp 0.8s var(--ease-out) 0.15s both;
}
```

注意：副标题中 `·` 前后要有空格。

#### 8.5 两个入口卡片（核心！当前最难看的地方）

**卡片容器** `.home-cards`：
```css
.home-cards {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 28px;
  max-width: 720px;
  width: 100%;
  padding: 0 16px;
  animation: fadeUp 0.8s var(--ease-out) 0.3s both;
}
@media (max-width: 600px) {
  .home-cards { grid-template-columns: 1fr; max-width: 380px; }
}
```

**单张卡片** `.home-card`（玻璃拟态效果）：
```css
.home-card {
  position: relative;
  background: var(--glass-bg);
  backdrop-filter: var(--glass-blur);
  -webkit-backdrop-filter: var(--glass-blur);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-lg);
  padding: 48px 36px;
  cursor: pointer;
  transition: all 0.4s var(--ease-out);
  overflow: hidden;
  text-decoration: none;
  display: block;
}

/* 卡片内部微妙的光泽层 */
.home-card::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 1px;
  background: linear-gradient(90deg,
    transparent 0%,
    rgba(212,197,176,0.08) 50%,
    transparent 100%);
  opacity: 0;
  transition: opacity 0.4s;
}

/* Hover 效果 */
.home-card:hover {
  transform: translateY(-6px);
  border-color: rgba(201,168,108,0.2);
  box-shadow:
    0 20px 60px rgba(15,20,30,0.4),
    0 0 40px rgba(201,168,108,0.08),
    inset 0 1px 0 rgba(212,197,176,0.06);
}
.home-card:hover::before { opacity: 1; }
```

**卡片顶部图标区**（不要 emoji！用文字符号）：
- 卡片 A "探索你的电影性格"：用一个大的罗马数字 **"I"** 或文字 **"PROFILE"**
- 卡片 B "今天该看什么？"：用罗马数字 **"II"** 或文字 **"TODAY"**

```css
.card-symbol {
  font-family: var(--font-serif-en);
  font-size: 14px;
  font-weight: 600;
  letter-spacing: 0.25em;
  color: var(--gold-dim);
  margin-bottom: 28px;
  opacity: 0.7;
}
```

**卡片标题**：
```css
.card-title {
  font-family: var(--font-serif-zh);
  font-size: 22px;
  font-weight: 600;
  color: var(--text-bright);
  margin-bottom: 16px;
  line-height: 1.4;
}
```

**卡片描述文字**：
```css
.card-desc {
  font-size: 14px;
  font-weight: 300;
  color: var(--text-secondary);
  line-height: 1.8;
  margin-bottom: 28px;
}
```

**卡片底部信息栏**（显示时间/步骤）：
```css
.card-meta {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 12px;
  color: var(--text-muted);
  letter-spacing: 0.05em;
}

.card-meta::before {
  content: '→';
  color: var(--gold-dim);
  font-size: 10px;
}
```

**删除"经典入口"标签！** 不要任何额外的 badge 或标签。

#### 8.6 底部引用文案
```css
.home-footer-quote {
  margin-top: 80px;
  font-family: var(--font-serif-zh);
  font-size: clamp(13px, 1.3vw, 15px);
  font-style: italic;
  font-weight: 400;
  color: var(--text-faint);
  letter-spacing: 0.15em;
  text-align: center;
  animation: fadeUp 0.8s var(--ease-out) 0.45s both;
  position: relative;
  padding: 0 24px;
}

/* 装饰性引号 */
.home-footer-quote::before {
  content: '"';
  font-size: 24px;
  color: var(--gold-glow-strong);
  position: absolute;
  left: 0;
  top: -8px;
}
.home-footer-quote::after {
  content: '"';
  font-size: 24px;
  color: var(--gold-glow-strong);
  position: absolute;
  right: 0;
  bottom: -16px;
}
```

### 【9】电影选择页 MovieSelection 升级

#### 9.1 页面标题区域
```css
.selection-header {
  text-align: center;
  padding: 48px 24px 24px;
}
.selection-header h1 {
  font-family: var(--font-serif-zh);
  font-size: 28px;
  font-weight: 600;
  color: var(--text-bright);
  margin-bottom: 8px;
}
.selection-header p {
  font-size: 14px;
  color: var(--text-muted);
  font-weight: 300;
}
```

#### 9.2 分类标签（药丸形状）
```css
.category-tabs {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 8px;
  padding: 16px 24px 24px;
  max-width: 800px;
  margin: 0 auto;
}

.category-tab {
  padding: 8px 20px;
  border-radius: 100px;
  font-size: 13px;
  font-weight: 500;
  color: var(--text-secondary);
  background: transparent;
  border: 1px solid var(--border-subtle);
  cursor: pointer;
  transition: all 0.25s var(--ease-out);
  font-family: var(--font-sans);
  letter-spacing: 0.02em;
}

.category-tab:hover {
  color: var(--text-primary);
  border-color: var(--border-default);
  background: var(--bg-hover);
}

.category-tab.active {
  color: var(--bg-base);
  background: var(--gold);
  border-color: var(--gold);
  font-weight: 600;
  box-shadow: 0 2px 12px rgba(201,168,108,0.25);
}
```

#### 9.3 电影网格布局
```css
.movie-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
  gap: 16px;
  padding: 16px 24px 140px;
  max-width: 1100px;
  margin: 0 auto;
}
@media (max-width: 600px) {
  .movie-grid { grid-template-columns: repeat(auto-fill, minmax(130px, 1fr)); gap: 12px; }
}
@media (max-width: 400px) {
  .movie-grid { grid-template-columns: repeat(2, 1fr); }
}
```

#### 9.4 底部固定操作栏
```css
.sticky-bottom {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: rgba(21,25,38,0.92);
  backdrop-filter: blur(20px);
  border-top: 1px solid var(--border-subtle);
  padding: 16px 32px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  z-index: 100;
}
.count-text {
  font-size: 14px;
  color: var(--text-secondary);
}
.count-text strong {
  color: var(--gold);
  font-weight: 600;
}
.btn-primary {
  background: linear-gradient(135deg, var(--gold) 0%, var(--gold-dim) 100%);
  color: var(--bg-base);
  border: none;
  padding: 12px 36px;
  border-radius: 100px;
  font-size: 14px;
  font-weight: 600;
  font-family: var(--font-sans);
  cursor: pointer;
  transition: all 0.3s var(--ease-out);
  letter-spacing: 0.05em;
}
.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(201,168,108,0.3);
}
.btn-primary:disabled {
  opacity: 0.4;
  cursor: not-allowed;
  transform: none;
  box-shadow: none;
}
```

#### 9.5 进度条
```css
.progress-bar {
  width: 120px;
  height: 3px;
  background: var(--bg-elevated);
  border-radius: 3px;
  overflow: hidden;
  margin-left: 12px;
  display: inline-block;
  vertical-align: middle;
}
.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--gold-dim), var(--gold));
  border-radius: 3px;
  transition: width 0.3s var(--ease-out);
}
```

### 【10】其他页面统一深色适配

以下每个页面都要检查并确保：

| 页面文件 | 必须检查的项目 |
|----------|---------------|
| TagConfirmation.jsx | 背景--bg-deep、卡片--glass-bg+backdrop-filter、标签pill风格同上 |
| PersonalityProfile.jsx | 背景深色、雷达图区域背景--bg-card、文字浅色、按钮金色 |
| Recommendation.jsx | 背景深色、推荐电影首字卡片、文字浅色 |
| DailyContext.jsx | 背景深色、选择器/下拉菜单深色风格、选项卡片玻璃拟态 |
| DailyResult.jsx | 背景深色、翻卡保持但卡片内用首字+文字、推荐语优雅排版 |

**通用规则：不允许出现 #ffffff 白色背景或接近白色的浅色背景。所有背景必须使用 --bg-deep / --bg-base / --bg-card / --glass-bg 这些变量。配色已参考 Letterboxd(#14181C) 和 MUBI 调亮，背景是深蓝灰暖色，不是纯黑。**

### 【11】动效系统

在 index.css 中追加以下全局动画关键帧：
```css
/* 交错淡入（用于列表项逐个出现） */
@keyframes staggerIn {
  from { opacity: 0; transform: translateY(16px); }
  to   { opacity: 1; transform: translateY(0); }
}

.movie-card {
  animation: staggerIn 0.4s var(--ease-out) both;
}
/* 在 JSX 中通过 style={{animationDelay: `${index * 50}ms`}} 设置延迟 */

/* 脉冲光晕（用于选中态） */
@keyframes softPulse {
  0%, 100% { box-shadow: 0 0 20px rgba(201,168,108,0.12); }
  50%      { box-shadow: 0 0 28px rgba(201,168,108,0.2); }
}

.movie-card.selected {
  animation: softPulse 3s ease-in-out infinite;
}
```

---

## 四、移动端适配

### 【12】响应式断点处理

在 index.css 追加：
```css
/* 手机端 (< 480px) */
@media (max-width: 480px) {
  .home-card { padding: 32px 24px; }
  .card-title { font-size: 19px; }
  .card-desc { font-size: 13px; }
  .search-input { width: 100%; }
  .category-tabs { gap: 6px; padding: 12px 16px; }
  .category-tab { padding: 6px 14px; font-size: 12px; }
  .sticky-bottom { padding: 12px 20px; }
  .btn-primary { padding: 10px 24px; font-size: 13px; }
  body { font-size: 15px; }
}

/* 确保触控区域够大 */
button, .category-tab, .home-card, .movie-card {
  min-height: 44px;
  -webkit-tap-highlight-color: transparent;
}
```

---

## 五、局域网访问配置

### 【13】vite.config.js 修改
确保 vite.config.js 包含：
```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    host: '0.0.0.0',
    port: 3000
  }
})
```

---

## 六、总结：修改优先级

请按以下顺序执行（前面的影响后面）：

1. ✅ 先改 index.html 加 Google Fonts
2. ✅ 再改 index.css 全局变量和基础样式
3. ✅ 重写 HomePage.jsx（视觉效果最大提升点）
4. ✅ 改 movies.json 清理 poster 为空
5. ✅ 改 MovieSelection.jsx（卡片 + 分类标签 + 底部栏）
6. ✅ 添加搜索功能
7. ✅ 其他页面统一深色适配
8. ✅ 移动端适配
9. ✅ vite.config.js 加 host 配置
10. ✅ 修 movies.json 格式错误
11. ✅ 修复分类过滤逻辑

**每完成一步都可以刷新浏览器查看效果，不需要等全部做完。**
