name: Legacy GitHub Pages deployment (disabled)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deployment is disabled. Cloudflare Pages is the only production environment."
