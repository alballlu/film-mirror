# Frontend requests use the same-origin Cloudflare Pages Function by default.
# Override only when developing against another compatible proxy.
VITE_TMDB_API_URL=/api/tmdb-proxy

# Never put TMDB_API_KEY or VITE_TMDB_API_KEY in this file.
# Configure TMDB_API_KEY as an encrypted runtime secret in Cloudflare Pages.
