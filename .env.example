# ===== TMDB API Key（必填）=====
# 所有部署方式都需要：Cloudflare Pages、GitHub Pages、本地开发
VITE_TMDB_API_KEY=your_tmdb_api_key_here

# ===== Cloudflare Pages 代理模式（国内推荐）=====
# Cloudflare Pages 部署时必须设置此变量（在 Cloudflare Dashboard 中添加）
# 前端走 /api/tmdb-proxy 代理，绕过中国大陆 TMDB 被墙问题
# 代理失败时自动回退到直连（使用上面 VITE_TMDB_API_KEY 兜底）
# 取消下面的注释以启用代理模式：
# VITE_TMDB_API_URL=/api/tmdb-proxy

# ===== 本地开发 =====
# 本地运行时，复制此文件为 .env.local，设置 VITE_TMDB_API_KEY 即可
# 本地开发不需要代理（直连 TMDB）